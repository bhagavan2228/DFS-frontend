# Local development: leave unset — the app uses /api and Vite proxies to the backend (vite.config.js).
# VITE_API_URL=

# Production static hosting with API on another origin (build-time variable):
# VITE_API_URL=https://api.yourdomain.com/api
